# Clarification Log: AutoCharge FreeCharge

**Feature Slug:** autocharge-freecharge
**Last Updated:** 2026-05-12

## Blocking Questions For Current Review Cycle

No blocking clarification questions remain for the current review cycle.

## Non-Blocking, Deferred, Or Resolved Items

| ID | Question Or Conflict | Source IDs | Impact Area | Status | Answer Or Decision | Decision Owner | Date |
|----|----------------------|------------|-------------|--------|--------------------|----------------|------|
| CL-001 | What are the business priority and required PO/TPM acceptance evidence for this FreeCharge campaign? | SRC-001, SRC-003, SRC-005 | scope, success criteria, handoff | Answered | Priority is P0 / launch-blocking for the planned 2026-06-10 stage campaign. Acceptance evidence must be a durable PO/TPM record confirming the product spec, supporting artifacts, and Spec Kit input split are acceptable before `product-spec.md` can be marked `Accepted`. | PO/TPM | 2026-05-12 |
| CL-003 | What are the Retail eligibility timeout, pending retry interval, pending retry threshold, and retry tracking source for `EligibilityPending` orders? | SRC-001, SRC-003, SRC-005 | integration, error handling, observability | Answered | Synchronous 203 Checkout Retail timeout is 3 seconds with no inline retry. Pending retry runs every 5 minutes and stops after 12 attempts or 60 minutes pending age, whichever comes first. Warn after 30 minutes; manual recovery after 60 minutes or 12 failed attempts. v1 does not add a trace table; pending age uses existing order timestamps and retry evidence uses scheduler/job logs linked by `order_id` and `request_id`. | TPM | 2026-05-12 |
| CL-005 | Confirm the remaining product behavior for Retail `charging_time` wire format, invalid vehicle UUID/Retail 4xx responses, and the CPO/LXM response body or code for FreeCharge and pending states. | SRC-001, SRC-002, SRC-003, SRC-004, SRC-005 | API, integration, workflow, error handling | Answered | Retail `charging_time` is RFC3339 UTC derived from successful 202 ValidateCar `requestTime`. Retail 4xx caused by missing or invalid vehicle UUID creates `EligibilityPending`, blocks payment and invoice, and alerts for recovery; it is not treated as not eligible and does not fall back to payment. FreeCharge and `EligibilityPending` preserve the current 203 Checkout success contract: HTTP 200, code `00`, message `success`, existing response schema, and `data.amount` remains the original checkout amount. Duplicate checkout preserves `EC215`. Existing 202/203 HTTP contracts must not change. | TPM | 2026-05-12 |
| CL-006 | Should 202 ValidateCar include a FreeCharge precheck so FreeCharge vehicles can start charging without a bound card or with unpaid orders? | SRC-001 | scope, workflow | Deferred | Current version excludes 202 as the formal FreeCharge decision point. Treat any 202 FreeCharge precheck as a future feature unless PO/TPM reopens scope. | PO/TPM | 2026-05-11 |
| CL-007 | Should AutoCharge persist a FreeCharge trace table or campaign traceability fields in v1? | SRC-001 | data, audit | Deferred | Current version explicitly defers trace table and campaign traceability fields. | PO/TPM | 2026-05-11 |
| CL-008 | Are `api_type = 202` and `api_type = 203` FreeCharge statuses or order statuses? | SRC-001 | workflow, data | Answered | They are request-log flow markers only. They are not FreeCharge statuses and are not order statuses. | Source decision | 2026-05-11 |
| CL-009 | Is `lxm_cpo_orders.order_status` the correct database/model field name for the final order-level decision? | SRC-002 | data, compatibility | Answered | No. Current FVS-VAS code uses `lxm_cpo_orders.status`; future spec language should use `status` when referring to the current model/DB field. | Codebase inspection | 2026-05-12 |
| CL-010 | Should negative checkout amount be treated as `NoCharge`? | SRC-002 | API, validation | Answered | No. Current 203 Checkout DTO validates `amount` as `gte=0`; `amount == 0` maps to `NoCharge`, and negative amount is invalid input. | Codebase inspection | 2026-05-12 |
| CL-011 | What physical enum or database values shall represent the logical FreeCharge and pending statuses, and are any compatibility or migration rules required? | SRC-001, SRC-002, SRC-003 | data, workflow, compatibility | Answered | FreeCharge shall use the existing `NoChargePromotion` status (`LxmCpoOrderStatusNoChargePromotion = 12`). `EligibilityPending` shall be added as a new order status. The exact numeric enum/database value can be assigned during implementation unless TPM later requires a specific value. | PO/TPM | 2026-05-12 |
| CL-012 | For FreeCharge v1, should AutoCharge skip invoice entirely or issue a separate zero-amount invoice mode? | SRC-001, SRC-003 | payment, invoice, compliance | Answered | FreeCharge v1 shall not trigger payment deduction and shall not issue an invoice. Separate zero-amount invoice mode is out of scope unless PO/TPM reopens the decision. | PO/TPM | 2026-05-12 |
| CL-013 | Should an existing `EligibilityPending` FreeCharge order block later 202 ValidateCar requests if Retail remains unavailable past the customer's charging session? | SRC-006 | workflow, compatibility, customer charging continuity | Answered | No. A prior `EligibilityPending` FreeCharge order shall not block future 202 ValidateCar requests, shall not be treated as unpaid debt, failed payment, or outstanding payment, and shall remain an operations/retry recovery concern. 202 ValidateCar may still fail for existing non-FreeCharge blockers, but not solely because of `EligibilityPending`. | PO/TPM | 2026-05-12 |
| CL-014 | If a vehicle becomes FreeCharge-eligible after the successful 202 ValidateCar `requestTime` but before or during charging, should v1 retroactively grant FreeCharge? | SRC-007 | scope, eligibility boundary, charging time | Deferred | No for v1. Eligibility is evaluated against the successful 202 ValidateCar `requestTime` only. Charging-interval overlap, mid-session eligibility activation, 203 Checkout time, and retroactive campaign adjustment are deferred unless PO/TPM reopens scope. | PO/TPM | 2026-05-12 |
| CL-015 | What is the current credit-card validation boundary, and should FreeCharge v1 change it? | SRC-002 | workflow, payment, compatibility | Answered | Current 202 ValidateCar checks bound-card availability and card expiry before charging starts; live ECPay authorization happens later only for `Unpaid` orders after 203 Checkout. FreeCharge v1 preserves this boundary and does not add a 202 FreeCharge precheck or relax existing card/outstanding-payment blockers. | Codebase inspection; PO/TPM compatibility constraint | 2026-05-12 |
